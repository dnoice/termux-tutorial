---
title: Root Without Rooting
description: What PRoot actually is — ptrace-based syscall interception, not virtualisation — and why that answers "why not just root the phone".
sidebar:
  order: 1
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: What PRoot actually is — ptrace-based syscall interception, not virtualisation — and why that answers "why not just root the phone". */}

Coming up: what PRoot actually is — ptrace-based syscall interception, not virtualisation — and why that answers "why not just root the phone".

<LessonComplete client:only="react" slug="container/why-proot" />
