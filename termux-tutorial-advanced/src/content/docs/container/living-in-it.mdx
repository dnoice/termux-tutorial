---
title: Living in the Container
description: Two package managers, two homes, two sets of paths. Moving files across the boundary without losing them.
sidebar:
  order: 3
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: Two package managers, two homes, two sets of paths. Moving files across the boundary without losing them. */}

Coming up: two package managers, two homes, two sets of paths. Moving files across the boundary without losing them.

<LessonComplete client:only="react" slug="container/living-in-it" />
