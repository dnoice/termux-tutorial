---
title: Installing a Full Distro
description: 'proot-distro: installing a real Debian rootfs, logging in, and what it costs in disk, battery and speed.'
sidebar:
  order: 2
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: proot-distro: installing a real Debian rootfs, logging in, and what it costs in disk, battery and speed. */}

Coming up: proot-distro: installing a real Debian rootfs, logging in, and what it costs in disk, battery and speed.

<LessonComplete client:only="react" slug="container/first-distro" />
