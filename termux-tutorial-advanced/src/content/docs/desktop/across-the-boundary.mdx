---
title: The Desktop in the Container
description: Server on the host, session in the container, a shared socket as the tether. The hardest setup in the series.
sidebar:
  order: 6
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: Server on the host, session in the container, a shared socket as the tether. The hardest setup in the series. */}

Coming up: server on the host, session in the container, a shared socket as the tether. The hardest setup in the series.

<LessonComplete client:only="react" slug="desktop/across-the-boundary" />
