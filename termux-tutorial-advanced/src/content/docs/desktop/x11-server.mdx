---
title: A Display Server on Android
description: Termux:X11 — the display server on its own, proven working before any desktop is involved.
sidebar:
  order: 4
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: Termux:X11 — the display server on its own, proven working before any desktop is involved. */}

Coming up: termux:X11 — the display server on its own, proven working before any desktop is involved.

<LessonComplete client:only="react" slug="desktop/x11-server" />
