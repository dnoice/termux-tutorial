---
title: Bringing Up XFCE
description: 'XFCE4 on the Termux side: install, DISPLAY, the session bus, and what a first launch actually looks like.'
sidebar:
  order: 5
---

import LessonComplete from '../../../components/profile/LessonComplete.tsx';

{/* STUB — created so the sidebar, the progress dashboard and the link
    check all resolve. Brief: XFCE4 on the Termux side: install, DISPLAY, the session bus, and what a first launch actually looks like. */}

Coming up: xFCE4 on the Termux side: install, DISPLAY, the session bus, and what a first launch actually looks like.

<LessonComplete client:only="react" slug="desktop/xfce" />
