# Where we left off — 2026-08-12, ~00:45

Goodnight, partner. Everything is committed and pushed; nothing is half-finished
in the working tree. Here is the state, shortest-path first.

## Two switches, and the site is live

Both are on GitHub, neither is in the code. They are **independent** — fixing
one does not clear the other:

1. **Settings → Pages → Source = "GitHub Actions"** (not "Deploy from a branch").
2. **Make the repository public.** That is the plan of record rather than Pro.

The last run — [`31575238525`](https://github.com/dnoice/termux-tutorial/actions) —
passed **every step**: HMR guard, asset-sync guard, four installs, four
typechecks, four builds, assembly, and cross-course link verification. It failed
only at `Configure Pages`, which is switch 1 above. That failure happens inside
the *build* job, before the deploy job starts, which is why a green build log
still ends in a red run. Nothing to fix in the repo.

Pre-public check ran clean across the **whole history**, not just the current
tree: no secrets in any commit, no key or credential files, no absolute local
paths, nothing ignored-but-tracked. Recorded with its date in the README.

## What changed today

- **The documentation audit landed.** 414 findings across 14 regions; the report
  and the raw evidence are in `global-docs/audits/repo/`. Everything P1 and P2 is
  fixed. The learner-facing ones mattered most: the Advanced course used to
  congratulate you for finishing Intermediate, and Intermediate linked to a
  repository that does not exist.
- **Four invariants became executable** instead of comments asking to be
  remembered: `teaches[]` coverage, `SANDBOX_PATH` resolving to a real page, HMR
  path uniqueness, and shared-asset sync. Each one I broke on purpose to prove it
  fails.
- **One assets directory.** `global-assets/` is where you edit; copies are
  generated by `npm run assets:sync` and guarded by `npm run assets:check`.
- **Your boot splash is in.** Ported to the hub and live. It is better than what
  I had, for reasons written up in `hub/CLAUDE.md` — chiefly that the chips are
  *caused* by the traces landing rather than staggered into place, and that the
  engine never rewrites your artwork the way mine did.
- **The first drawing is retired** to `scratchpad/retired-artwork/`, on disk and
  out of the repo.
- **Licence settled**: BSD-3-Clause in all nine places that declare one.
  Third-party icon attribution stays beside the assets it describes.

## Waiting on you, not on me

- **`global-assets/retired-backgrounds/`** — about 7 MB of superseded artwork
  that nothing references, and the bulk of what remains in the repo. It is your
  artwork, so deleting it is your call, not mine.
- **Per-course splashes.** You said "another day". The component sits unmounted
  in all three courses, so it is a mount plus some copy, not a rebuild.
- **Git history still carries the old blobs** from before the scratchpad move.
  The working tree is 1.6 MB; `.git` is larger. Rewriting history would shrink
  the clone but it is invasive, so I did not touch it.

## Picking it back up

```bash
npm run dev        # all four projects, one URL: localhost:4321/termux-tutorial/
npm run build      # builds all four, assembles, verifies every cross-course link
npm run check      # typechecks all four
```

The dev server is running now; `Ctrl-C` in that terminal stops all four. If a
port is stuck from an earlier session, `astro dev stop` inside the project
clears it — Astro daemonizes, so servers outlive the shell that started them.

**One honest note for the morning.** The remaining audit tail is P3 and below —
real but cosmetic. I would not start there. The first thing worth doing is
flipping those two switches and looking at the deployed site, because everything
above has only ever been verified locally and in CI.

Sleep well.
